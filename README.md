# Manu Pal — Portfolio

Static portfolio built with HTML and CSS. Deployed on Vercel as a static site.

## Local
- Open `index.html` in your browser.

## Deploy (Vercel)
- Import this repo into Vercel as a static project, or use the Vercel CLI.
- No build step required; root directory is the output.
